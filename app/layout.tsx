import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'FinDocGPT - AI-Powered Financial Intelligence Platform',
  description: 'Advanced financial analysis, market data, AI-powered insights, and trading intelligence platform',
  keywords: 'finance, AI, trading, market analysis, cryptocurrency, stocks, forex',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
